---
"@autono/pinbox": minor
"@autono/pinbox-toolbar": minor
---

Add local worktree preview registration and an opt-in Vite toolbar switcher. Preview choices show readiness, PR links, and backend sharing labels; navigation preserves the page and keeps feedback in each worktree's hub. No branch checkout or backend deployment is performed.
