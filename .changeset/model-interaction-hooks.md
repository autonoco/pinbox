---
"@autono/pinbox-toolbar": minor
---

Add optional 3D hover, activation and camera hooks with cleanup, cancellation, multi-viewer capture ownership and the existing 2D capture fallback. Expose openPin for host sidebar navigation.
