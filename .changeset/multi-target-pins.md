---
"@autono/pinbox-core": minor
"@autono/pinbox-toolbar": minor
---

Multi-target pins: while placing, shift+click captures extra elements into the pending pin (numbered dashed outlines are the receipt) and a plain click commits the pin with all of them — the committing click stays the anchor for the marker and needle. Additive `targets` array on the Target schema (one level deep). The card lists every locus, and both markdown serializers carry them — the compact headline briefs inject now shows the pattern ("+2 more rows"), not one instance.
