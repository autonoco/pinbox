---
"@autono/pinbox-toolbar": minor
---

Resolved pins are no longer a dead end: a verified-resolved pin's card now offers Unresolve (the same hub reopen the verify prompt uses), so anything in the inbox's RESOLVED tab can be flipped back to open.
