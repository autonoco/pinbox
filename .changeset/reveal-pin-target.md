---
"@autono/pinbox-toolbar": patch
---

Reveal an inbox pin's DOM target inside nested scroll containers before opening its card. Same-origin pins on another page reopen after navigation and content arrival. Query parameter ordering no longer hides matching anchors; page-level notes are labeled PAGE.
