---
"@autono/pinbox-core": minor
"@autono/pinbox": minor
"@autono/pinbox-toolbar": minor
---

Add reusable revision-qualified 3D model anchors, CLI creation and trusted-backend author attribution, and a renderer-neutral helper that follows part transforms during exploded-view animations. Preserve existing DOM/source pin formats and expose stale or missing anchors explicitly.
