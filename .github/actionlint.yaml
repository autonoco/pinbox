# actionlint only knows GitHub-hosted labels; every custom runner label used by the
# workflows must be listed here or actionlint flags it as unknown.
self-hosted-runner:
  labels:
    - blacksmith-4vcpu-ubuntu-2404
