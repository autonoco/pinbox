# docs/design/cli

Historical CLI UX design artifacts (the CLI is the primary surface): literal terminal
transcripts of every command's human output, error cases with hints, and `--help`. These
encoded the output contract before it was implemented, and parts have since drifted — for
the surface as shipped, see `docs/cli/commands.mdx`.
