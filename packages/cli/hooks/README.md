# cli/hooks

Shipped hook payloads: Claude Code SessionStart (register session), UserPromptSubmit (inject open pins), Stop (hold before finishing if pins arrived mid-turn), plus the git post-commit trailer hook. Installed into target repos by `pinbox init`.
